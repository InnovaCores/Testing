// shared.steps.js
const { Given, Then } = require('@cucumber/cucumber');

// Contexto general
Given('que el {string} está en la sección {string}', function (rol, seccion) {
    // Navegación genérica
});

// Confirmaciones
Then('el sistema solicita confirmación', function () {
    // Verificar diálogo de confirmación
});

// Estados del sistema
Given('al menos un usuario aceptó su invitación para unirse al proyecto', function () {
    // Configurar estado con usuario
});